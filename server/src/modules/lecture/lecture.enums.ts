export enum LectureType {
  VIDEO = "video",
  ARTICLE = "article",
  QUIZ = "quiz",
}

export enum LectureStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
}

export enum ResourceType {
  PDF = "pdf",
  ZIP = "zip",
  LINK = "link",
  OTHER = "other",
}